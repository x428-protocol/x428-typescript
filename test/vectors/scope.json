[
  {
    "description": "Exact match — identical URIs",
    "category": "scope",
    "input": {
      "tokenScope": "https://example.com/api/resource",
      "requestedResource": "https://example.com/api/resource"
    },
    "expected": {
      "matches": true
    }
  },
  {
    "description": "Prefix wildcard — single segment matches",
    "category": "scope",
    "input": {
      "tokenScope": "https://example.com/api/*",
      "requestedResource": "https://example.com/api/data"
    },
    "expected": {
      "matches": true
    }
  },
  {
    "description": "Prefix wildcard — nested path rejected (extra / not allowed)",
    "category": "scope",
    "input": {
      "tokenScope": "https://example.com/api/*",
      "requestedResource": "https://example.com/api/v2/data"
    },
    "expected": {
      "matches": false
    }
  },
  {
    "description": "Completely different path — no match",
    "category": "scope",
    "input": {
      "tokenScope": "https://example.com/api/resource",
      "requestedResource": "https://example.com/other/thing"
    },
    "expected": {
      "matches": false
    }
  },
  {
    "description": "Query string ignored — matches base URI",
    "category": "scope",
    "input": {
      "tokenScope": "https://example.com/api/resource",
      "requestedResource": "https://example.com/api/resource?key=value"
    },
    "expected": {
      "matches": true
    }
  },
  {
    "description": "Fragment ignored — matches base URI",
    "category": "scope",
    "input": {
      "tokenScope": "https://example.com/api/resource",
      "requestedResource": "https://example.com/api/resource#section"
    },
    "expected": {
      "matches": true
    }
  },
  {
    "description": "Different scheme — no match",
    "category": "scope",
    "input": {
      "tokenScope": "https://example.com/api/resource",
      "requestedResource": "http://example.com/api/resource"
    },
    "expected": {
      "matches": false
    }
  },
  {
    "description": "Different host — no match",
    "category": "scope",
    "input": {
      "tokenScope": "https://example.com/api/data",
      "requestedResource": "https://other.com/api/data"
    },
    "expected": {
      "matches": false
    }
  }
]
